import React from 'react'

const TaskCard = () => {
    return (
        <div>
           `${props.id},${props.status},${props.category},${props.title},` 
        </div>
    )
}

export default TaskCard;
